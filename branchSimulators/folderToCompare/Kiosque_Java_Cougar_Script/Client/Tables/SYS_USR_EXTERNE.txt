##
SYS_USR_EXTERNE
=SQL IFNOTEXIST=SYS_USR_EXTERNE=
CREATE TABLE SYS_USR_EXTERNE ( LOGIN VARCHAR2(32) NOT NULL ENABLE, EMAIL VARCHAR2(255), LOGON_HOST_WHEN_NS VARCHAR2(8), NOMFAM VARCHAR2(50), PRENOM VARCHAR2(32), PRIMARY KEY (LOGIN) ) 
