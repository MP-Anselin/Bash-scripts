##
VARSYS_MODELE
=SQL=
CREATE OR REPLACE VIEW VARSYS_MODELE AS
SELECT s.MODULE,s.CODVAR,s.VALVAR,s.DESCRIPTION,s.TYPVAR,DECODE(b.CODVAR,NULL,'Non','Oui') AS BLOQUE
FROM SYS_VARSYS@KRHMODELE s LEFT JOIN SYS_VARSYS_BLOQUE@KRHMODELE b ON s.CODVAR = b.CODVAR
ORDER BY s.MODULE,s.CODVAR
