##
TAG_CHARSET=@€ιθη
##
RELOAD_R_PRESENT_FICHIER_PAIE
=SQL=
DECLARE
CURSOR cur_pac IS SELECT NUMPAC FROM TGE_REF_PAC;
BEGIN
  FOR current_pac IN cur_pac
  LOOP
    R_PRESENT_FICHIER_PAIE(current_pac.NUMPAC, null);
  END LOOP;
END;
##
RELOAD_R_PRESENT_FICHIER_PAIE
=SQL=
DECLARE
CURSOR cur_pac IS SELECT NUMPAC FROM TGE_REF_PAC;
BEGIN
  FOR current_pac IN cur_pac
  LOOP
    R_PRESENT_FICHIER_PAIE(current_pac.NUMPAC, null);
  END LOOP;
END;
