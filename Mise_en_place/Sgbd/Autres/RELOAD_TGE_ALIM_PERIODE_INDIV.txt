##
TAG_CHARSET=@€ιθη
##
RELOAD_TGE_ALIM_PERIODE_INDIV
=SQL=
DECLARE
CURSOR cur_pac IS SELECT NUMPAC,PERIODEENCOURS FROM TGE_REF_PAC;
BEGIN
  FOR current_pac IN cur_pac
  LOOP
    TGE_ALIM_PERIODE_INDIV(0, current_pac.NUMPAC, current_pac.PERIODEENCOURS, null, null);
  END LOOP;
END;
##
RELOAD_TGE_ALIM_PERIODE_INDIV
=SQL=
DECLARE
CURSOR cur_pac IS SELECT NUMPAC,PERIODEENCOURS FROM TGE_REF_PAC;
BEGIN
  FOR current_pac IN cur_pac
  LOOP
    TGE_ALIM_PERIODE_INDIV(0, current_pac.NUMPAC, '200001', null, null);
  END LOOP;
END;
##
RELOAD_TGE_ALIM_PERIODE_INDIV
=SQL=
DECLARE
CURSOR cur_pac IS SELECT NUMPAC,PERIODEENCOURS FROM TGE_REF_PAC;
BEGIN
  FOR current_pac IN cur_pac
  LOOP
    TGE_ALIM_PERIODE_INDIV(0, current_pac.NUMPAC, '200001', null, null);
  END LOOP;
END;
